/**
 * Épico 1 — Shell autenticado: sidebar (MENU_LATERAL) + cabeçalho com saudação,
 * tag de ambiente, seletor de perfil e botão "Sair".
 */
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
